using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using CodeMonkey.Utils;
using CodeMonkey;

public class testing_grid : MonoBehaviour
{
    /*private Grid_map grid;


    private void Start()
    {
        grid = new Grid_map(27, 16, 1.5f, new Vector3(GameConfig.map_limits_x[0]-0.7f, GameConfig.map_limits_y, GameConfig.map_limits_z[0]));
    }

    private void Update()
    {
        
        grid.SetValue(MoveHead.moveObject.transform.position, 56);

            //Debug.Log(UtilsClass.GetMouseWorldPosition());
        
    }*/
}
